#include <stdio.h>
#include <stdlib.h>

#include "graph.h"

void quickSort() {

    return;
}

void primMST(Graph theGraph, int node_count, int edge_count) {
    if (theGraph == NULL) {
        return;
    }

    

    int (*arr)[3] = (int(*)[3])malloc((node_count - 1) * sizeof(int[3]));

    // 메모리 해제
    free(arr);
}

